{
  "name": "cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cypress run --browser chrome --env version=local",
    "start-staging": "cypress open --browser chrome --env version=local",
    "start-uat": "cypress open --browser chrome --env version=local",
    "start-prod": "cypress open --browser chrome --env version=local",
    "start:headed": "cypress run --headed --browser chrome --env version=local",
    "start:open": "cypress open --browser chrome --env version=local"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^13.14.0"
  },
  "devDependencies": {
    "cypress-mochawesome-reporter": "^3.8.2"
  }
}
